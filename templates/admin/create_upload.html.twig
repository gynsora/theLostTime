{% extends 'admin/base.html.twig' %}
{% block body %}
    <h1>Upload Image</h1>

    {{ form_start(createForm) }}
    {{ form_widget(createForm) }}
    <input type="submit" value="{% block btnValue %}Upload l'image{% endblock %}" class="btn btn-outline-success">
    {{ form_end(createForm) }}


{% endblock %}